export type RowProps = {
  schema: string
  color: string
}

export type SubSchemaProps = {
  annotations: string[]
  isOpen: boolean
}

export type AnnotationsCheckboxProps = {
  checked: boolean
  annotations: boolean[]
}
