{
    "configurations": [
    
        {
            "type": "chrome",
            "name": "localhost:3000",
            "request": "launch",
            "url": "localhost:3000",
            "webRoot": "${workspaceFolder}/src"
        }
    ]
}